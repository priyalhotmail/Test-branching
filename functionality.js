// Function to add two numbers passing as parameters
function addNumbers (no1, no2){
    console.log((no1 + no2));
}

// Define total variable to store the calculated value
const total = addNumbers(12, 11);

// Print total into console
console.log(total);
